# Пакет agent.llm: импортируйте из подпакетов (`agent.llm.clients.actor`, `agent.llm.prompts`, …).
# Реэкспорты на уровне пакета не используются — так `agent.llm.services.*` не тянет `openai` при тестах парсера.
